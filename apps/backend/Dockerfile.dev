FROM node:18-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
COPY apps/backend/package.json ./apps/backend/
COPY packages ./packages

RUN npm ci

COPY . .

WORKDIR /app

EXPOSE 3001

CMD ["sh", "-c", "cd apps/backend && npx prisma generate && npm run dev"]

